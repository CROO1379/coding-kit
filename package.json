{
  "name": "coding-kit",
  "version": "1.0.0",
  "description": "A modern static site starter kit with Vite, Pug, and Sass",
  "type": "module",
  "scripts": {
    "dev": "run-s clean:dev build:dev && run-p dev:serve dev:watch",
    "dev:serve": "browser-sync start --server dist --port 3000 --files 'dist/**/*' --no-ghost-mode --no-notify",
    "dev:watch": "node scripts/build.js --watch",
    "build": "run-s clean:dev build:dev",
    "build:dev": "node scripts/build.js",
    "clean": "rm -rf dist",
    "clean:dev": "rm -rf dist",
    "watch": "npm run dev:watch",
    "serve": "npm run dev:serve"
  },
  "keywords": [
    "vite",
    "pug",
    "sass",
    "static-site",
    "starter-kit"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "chokidar": "^4.0.3",
    "glob": "^11.0.0",
    "npm-run-all": "^4.1.5",
    "pug": "^3.0.3",
    "sass": "^1.77.0",
    "vite": "^5.4.0"
  }
}
